# Database migrations — reserved

Add ordered migrations only after the SQLite schema and transaction boundaries are implemented. No migration, database, or automatic migration runner exists yet. Track schema versions separately from trace schema versions.
