# Development cases — reserved

Define contract, initial fixture, allowed actions, expected outcomes, required checks, and budgets for each case. The sample projects are starting inputs, not a completed acceptance set.
