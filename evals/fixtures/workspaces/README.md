# Isolated evaluation workspaces — reserved

Create immutable synthetic baselines and copy one into a fresh temporary workspace per trial. Do not include graders or reference answers in the copy. examples/projects contains small illustrative starting projects.
