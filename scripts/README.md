# Developer scripts — reserved

Add scripts when there is a repeated verified workflow, such as trace validation, fixture isolation, or packaging checks. Prefer documented standard commands for the current scaffold; avoid scripts that imply missing application features work.
