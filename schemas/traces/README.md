# Trace schemas — reserved

Future trace envelope and event schemas belong here. Account for model inputs/raw outputs, structured policy records, clocks/counters, source manifests, HIL, replay divergence, and redaction. Existing planning_examples remain teaching artifacts until explicitly migrated.
