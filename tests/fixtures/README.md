# Test fixtures — reserved

Add minimal synthetic inputs here with tests that consume them. Do not copy private project files. Agent-visible workspaces must be explicitly isolated from expected outcomes and graders.
