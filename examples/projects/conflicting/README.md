# Conflicting-record fixture

The service uses PostgreSQL.

This statement is deliberately paired with a different checked-in default in settings.toml. The fixture provides no runtime observation, so the discrepancy cannot establish the running backend.
