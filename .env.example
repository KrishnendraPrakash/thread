# Documentation only: the scaffold does not load .env or make model requests.
# Local inference is intended to require no API key.
# Optional future provider credentials belong in your environment, not Git:
# THREAD_AGENT_API_KEY=
