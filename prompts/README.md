# Prompt drafts

The role files describe intended responsibilities; they are not active system prompts or evaluated templates. Concrete input/output schemas and prompt hashes will be introduced with the corresponding implementation. Keep source excerpts in separate untrusted evidence messages.

Contract selection, parsing, policy, and templated rendering may be deterministic and need no model call. Do not add a model invocation merely because a role file exists.
