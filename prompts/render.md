# Rendering role — draft

Render only checked claims and their explicit limitations. Preserve entity, scope, date, version, and checked citation details. Do not invent a more precise locator or convert an unresolved claim into a fact.

The runtime must check the actual resulting text after rendering before delivery. Where a deterministic template suffices, use that instead of a model call.
